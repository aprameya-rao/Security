# Copy this to `.env` and adjust for your lab.
# This is the Kafka broker address on the BRAIN VM (VM2).
KAFKA_BROKER=192.168.1.16:9092

# Optional: ack feedback topic the responder publishes kill results to.
KAFKA_ACK_TOPIC=kill_confirmations